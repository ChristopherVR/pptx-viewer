name: Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    # Run whenever CI completed (not just on full success) so a single broken
    # framework binding does not block releases for the others. The per-package
    # build steps below determine which packages are actually releasable.
    if: |
      github.event.workflow_run.conclusion == 'success' ||
      github.event.workflow_run.conclusion == 'failure'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      # Foundation packages must always build cleanly.
      - name: Build foundation packages
        run: |
          bun run --filter 'pptx-viewer-core' build
          bun run --filter 'pptx-viewer-shared' build
          bun run --filter 'pptx-viewer-mcp' build

      # Framework bindings: each can fail without blocking the others.
      - name: Build React
        id: build-react
        run: bun run --filter 'pptx-react-viewer' build
        continue-on-error: true

      - name: Build Vue
        id: build-vue
        run: bun run --filter 'pptx-vue-viewer' build
        continue-on-error: true

      - name: Build Angular
        id: build-angular
        run: bun run --filter 'pptx-angular-viewer' build
        continue-on-error: true

      # Decide, per package, whether it changed since its own last tag and what
      # its next INDEPENDENT version is. Each package carries its own version and
      # `<npm-name>@<version>` tag; unchanged packages are skipped entirely. See
      # scripts/release-plan.mjs. `--write` stamps each released version into its
      # package.json; the full plan is written to release-plan.json.
      - name: Plan release
        id: plan
        run: node scripts/release-plan.mjs --write

      # Regenerate each released package's own CHANGELOG.md from the commits that
      # touch it (or a bundled dependency), labelling unreleased commits with the
      # new per-package tag. git-cliff fully regenerates the file each time, so it
      # stays idempotent.
      - name: Generate per-package changelogs
        if: steps.plan.outputs.any_changed == 'true'
        run: |
          for key in core react vue angular tools; do
            release=$(node -p "require('./release-plan.json').packages.$key.release")
            [ "$release" = "true" ] || continue
            dir=$(node -p "require('./release-plan.json').packages.$key.dir")
            npm=$(node -p "require('./release-plan.json').packages.$key.npm")
            tag=$(node -p "require('./release-plan.json').packages.$key.tag")
            inc=$(node -e "console.log(require('./release-plan.json').packages.$key.includePaths.map(p => '--include-path ' + p).join(' '))")
            echo "Changelog for $npm -> $tag"
            bunx git-cliff --config cliff.toml --tag-pattern "^${npm}@" $inc --tag "$tag" --output "$dir/CHANGELOG.md"
          done

      # Regenerate the root CHANGELOG.md using all per-package and legacy v* tags
      # so it stays a consolidated view of every release across all packages.
      - name: Generate root changelog
        if: steps.plan.outputs.any_changed == 'true'
        run: |
          bunx git-cliff --config cliff-root.toml --output CHANGELOG.md

      - name: Commit changelogs
        if: steps.plan.outputs.any_changed == 'true'
        # The GITHUB_TOKEN push does not retrigger workflows, and `[skip ci]`
        # guards against it regardless. NOTE: if `main` is protected against
        # direct pushes, allow the actions bot or relax the rule.
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Stage per-package changelogs and the root consolidated changelog.
          # The --write version bumps are deliberately left uncommitted; tags + npm
          # are the source of truth for versions.
          git add packages/*/CHANGELOG.md CHANGELOG.md
          if git diff --cached --quiet; then
            echo "No changelog changes to commit"
          else
            git commit -m "docs(changelog): update package changelogs [skip ci]"
            git push origin HEAD:main
          fi

      # Pack each changed package and cut a per-package tag + GitHub release. The
      # tag points at the (pushed) changelog commit; the release body is that
      # package's newest changelog section. Each published release fires the
      # Publish workflow for exactly that one package.
      # A package is released only if it: (a) changed since its last tag AND
      # (b) its framework build succeeded (or it is a non-framework package).
      - name: Tag and release changed packages
        if: steps.plan.outputs.any_changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REACT_BUILT: ${{ steps.build-react.outcome == 'success' }}
          VUE_BUILT: ${{ steps.build-vue.outcome == 'success' }}
          ANGULAR_BUILT: ${{ steps.build-angular.outcome == 'success' }}
        run: |
          COMMIT=$(git rev-parse HEAD)
          CORE_VERSION=$(node -p "require('./release-plan.json').coreVersion")
          for key in core react vue angular tools; do
            release=$(node -p "require('./release-plan.json').packages.$key.release")
            [ "$release" = "true" ] || continue

            # Skip framework packages whose build failed this run.
            case "$key" in
              react)   [ "$REACT_BUILT"   = "true" ] || { echo "Skipping react: build failed"; continue; } ;;
              vue)     [ "$VUE_BUILT"     = "true" ] || { echo "Skipping vue: build failed";   continue; } ;;
              angular) [ "$ANGULAR_BUILT" = "true" ] || { echo "Skipping angular: build failed"; continue; } ;;
            esac

            dir=$(node -p "require('./release-plan.json').packages.$key.dir")
            packdir=$(node -p "require('./release-plan.json').packages.$key.packDir")
            version=$(node -p "require('./release-plan.json').packages.$key.version")
            tag=$(node -p "require('./release-plan.json').packages.$key.tag")

            if gh release view "$tag" >/dev/null 2>&1; then
              echo "Release $tag already exists, skipping"
              continue
            fi

            # Angular ships from its ng-packagr dist/: stamp the version, point
            # the core range at the live core version (which differs from angular's
            # own version, and from the release version when core did not change),
            # and copy the changelog into dist so it ships in the tarball.
            if [ "$key" = "angular" ]; then
              node -e "
                const fs = require('fs');
                const p = 'packages/angular/dist/package.json';
                const d = JSON.parse(fs.readFileSync(p, 'utf8'));
                d.version = '${version}';
                for (const f of ['dependencies', 'peerDependencies', 'optionalDependencies']) {
                  if (d[f] && d[f]['pptx-viewer-core']) d[f]['pptx-viewer-core'] = '^${CORE_VERSION}';
                }
                fs.writeFileSync(p, JSON.stringify(d, null, '\t') + '\n');
              "
              cp "$dir/CHANGELOG.md" packages/angular/dist/CHANGELOG.md
            fi

            (cd "$packdir" && bun pm pack)
            awk '/^## \[/{c++} c==1{print} c==2{exit}' "$dir/CHANGELOG.md" > "${RUNNER_TEMP}/notes.md"
            gh release create "$tag" \
              --target "$COMMIT" \
              --title "$tag" \
              --notes-file "${RUNNER_TEMP}/notes.md" \
              "$packdir"/*.tgz
            echo "Released $tag"
          done
