/**
 * Minimal global stylesheet written over the Vite starter's default styles:
 * the templates centre `#app` with padding and a max-width, which breaks a
 * full-viewport viewer. Used as `src/index.css` (React), `src/app.css`
 * (Svelte), and `src/style.css` (Vanilla).
 */
export const MINIMAL_APP_CSS = `:root {
  color-scheme: light dark;
}

body {
  margin: 0;
}
`;
