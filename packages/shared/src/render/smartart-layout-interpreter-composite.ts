/**
 * SmartArt DiagramML interpreter - composite (`composite`) arranger.
 *
 * Re-exported from `pptx-viewer-core`; see `smartart-layout-types.ts` for why
 * the interpreter moved there.
 */

export { arrangeComposite } from 'pptx-viewer-core';
