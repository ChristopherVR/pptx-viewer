/**
 * SmartArt DiagramML interpreter - linear (`lin`) and snake (`snake`) arrangers.
 *
 * Re-exported from `pptx-viewer-core`; see `smartart-layout-types.ts` for why
 * the interpreter moved there.
 */

export { arrangeLinear, arrangeSnake } from 'pptx-viewer-core';
