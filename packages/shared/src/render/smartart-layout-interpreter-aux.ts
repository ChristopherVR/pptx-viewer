/**
 * SmartArt DiagramML interpreter - auxiliary arrangers (`conn` / `sp` / `tx`).
 *
 * Re-exported from `pptx-viewer-core`; see `smartart-layout-types.ts` for why
 * the interpreter moved there.
 */

export { arrangeConn, arrangeSpacer, arrangeText } from 'pptx-viewer-core';
