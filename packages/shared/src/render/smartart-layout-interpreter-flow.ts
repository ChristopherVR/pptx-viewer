/**
 * SmartArt DiagramML interpreter - control-flow execution (forEach / choose).
 *
 * Re-exported from `pptx-viewer-core`; see `smartart-layout-types.ts` for why
 * the interpreter moved there.
 */

export { selectArrangedNodes, chooseAlgType } from 'pptx-viewer-core';
